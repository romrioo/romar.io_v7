---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Romario Jashari">
  <main>
    <article class="region prose wrapper hero">
      <h1>👋 welcome</h1>

      <h2>
       my name is Romario and I am a full-stack software <span class="text-gradient">developer</span> 
       <!-- <ahref="https://cube.fyi/">CUBE CSS</a> -->
      </h2>
    </article>

    <article class="region prose wrapper">
      <ul role="list" class="grid features">

      </ul>
    </article>

    <footer class="region prose wrapper">
      twentytwentythree <span role="img" aria-label="waving hand emoji"
        >2️⃣0️⃣2️⃣3️⃣</span
      >
    </footer>
  </main>
</Layout>

<style>
	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
	.instructions {
		line-height: 1.6;
		margin: 1rem 0;
		border: 1px solid rgba(var(--accent), 25%);
		background-color: white;
		padding: 1rem;
		border-radius: 0.4rem;
	}
	.instructions code {
		font-size: 0.875em;
		font-weight: bold;
		background: rgba(var(--accent), 12%);
		color: rgb(var(--accent));
		border-radius: 4px;
		padding: 0.3em 0.45em;
	}
	.instructions strong {
		color: rgb(var(--accent));
	}
	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(30ch, 1fr));
		gap: 1rem;
		padding: 0;
	}
</style>
